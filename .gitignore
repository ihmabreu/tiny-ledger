# --- Build output ----------------------------------------------------------
.gradle/
build/
# Per-architecture build directories, produced by the cross-architecture test
# runs documented in docs/TESTING.md.
build-*/
target/
*.class

# --- Quarkus ---------------------------------------------------------------
.quarkus/

# --- Gatling reports -------------------------------------------------------
load-tests/build/reports/gatling/

# --- Packaged artefacts ----------------------------------------------------
# The Gradle wrapper jar is deliberately committed so the build is reproducible
# on a machine with no Gradle installed.
*.jar
*.war
*.ear
*.zip
*.tar.gz
!gradle/wrapper/gradle-wrapper.jar

# --- Assessment source material --------------------------------------------
# The briefs this solution was written against. They are someone else's
# documents, not part of the deliverable, and are kept out of the repository.
*.pdf

# --- IDEs ------------------------------------------------------------------
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.project
.classpath
bin/

# --- JVM crash logs --------------------------------------------------------
hs_err_pid*
replay_pid*

# --- Logs & OS noise -------------------------------------------------------
*.log
.DS_Store
Thumbs.db
